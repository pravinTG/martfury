org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Windows multi-drive builds (pub cache on C:, project on D:) can trip Kotlin incremental compilation.
# Disable incremental to avoid intermittent daemon failures.
kotlin.incremental=false